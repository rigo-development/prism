{
    "name": "prism",
    "version": "0.0.0",
    "private": true,
    "workspaces": [
        "apps/*",
        "packages/*"
    ],
    "scripts": {
        "build": "npm run build --workspaces",
        "test": "npm run test --workspaces",
        "test:e2e": "npm run test:e2e -w apps/backend",
        "db:init": "npm run db:init -w apps/backend",
        "db:generate": "npm run db:generate -w apps/backend",
        "db:prod:sync": "npm run db:prod:sync -w apps/backend",
        "db:local:sync": "npm run db:generate && npm run db:init",
        "setup:local": "npm install && npm run build --workspaces && npm run db:local:sync",
        "setup:postgres": "npm install && npm run build --workspaces && npm run db:prod:sync",
        "setup": "npm run setup:local",
        "vercel-build": "npx prisma generate --schema=apps/backend/prisma/schema.prisma && npx prisma db push --schema=apps/backend/prisma/schema.prisma --accept-data-loss && npm run build --workspaces",
        "dev": "concurrently -n \"SHARED,BACKEND,FRONTEND\" -c \"yellow,blue,magenta\" \"npm run dev -w packages/shared\" \"npm run dev -w apps/backend\" \"npm run dev -w apps/frontend\""
    },
    "devDependencies": {
        "concurrently": "^9.2.1",
        "eslint": "^9.39.2",
        "prettier": "^3.7.4",
        "typescript": "^5.9.3"
    }
}